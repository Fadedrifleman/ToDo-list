<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>ToDo List</title>
</head>
<body>
    <% if (kindOfDay === "Sunday" || kindOfDay === "Saturday") { %>
        <h1 style="color: purple"><%= kindOfDay %>'s ToDo List </h1>
    <% } else { %>
        <h1 style="color: blue"><%= kindOfDay %>'s ToDo List </h1>
    <% } %>
</body> 
</html>